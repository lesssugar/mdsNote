#引入

```xml
<%@  taglib  uri="http://java.sun.com/jsp/jstl/core"  prefix="c"  %>
```

#使用

```java
<c:if test=""></c> 
<c:choose>
  	<c:when test=""></c:when>
  	<c:otherwise></c:otherwise>
</c:choose>
<c:forEach var="循环的变量名" items=""循环的集合 varStatus="x">	//x中包含的信息如当前循环到的下标等信息

```

